import React from 'react'

// Skeleton Navbar component for phase-1 refactor
export const Navbar: React.FC = () => {
  return (
    <div className="layout-navbar" aria-label="Navbar (skeleton)">
      Navbar (skeleton)
    </div>
  )
}
