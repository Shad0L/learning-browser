import React from 'react'

// Skeleton TabBar component for phase-1 refactor
export const TabBar: React.FC = () => {
  return (
    <div className="layout-tabbar" aria-label="TabBar (skeleton)">
      TabBar (skeleton)
    </div>
  )
}
